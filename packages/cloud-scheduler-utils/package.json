{
  "name": "@jackdbd/cloud-scheduler-utils",
  "version": "0.0.0-semantically-released",
  "description": "Utility functions to work with Cloud Sheduler",
  "author": "Giacomo Debidda <giacomo@giacomodebidda.com> (https://giacomodebidda.com/)",
  "license": "MIT",
  "keywords": [
    "predicate"
  ],
  "engines": {
    "node": ">=12.x",
    "npm": ">=7.0.0"
  },
  "type": "module",
  "module": "lib/index.js",
  "typings": "lib/index.d.ts",
  "exports": {
    ".": "./lib/index.js",
    "./create-jobs": "./create-jobs.js",
    "./delete-jobs": "./delete-jobs.js",
    "./random-schedule": "./random-schedule.js",
    "./package.json": "./package.json"
  },
  "typesVersions": {
    "*": {
      "*": [
        "./lib/*.d.ts"
      ]
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf coverage lib 'tsconfig.tsbuildinfo'",
    "format": "prettier --config ../../config/prettier.cjs --write {__tests__,src}/**/*.{js,mjs,ts}",
    "lint": "eslint --config ../../config/eslint.cjs",
    "nuke": "npm run clean && rimraf node_modules 'package-lock.json'",
    "precommit": "lint-staged --config ../../config/lint-staged.cjs",
    "prepublish:artifact-registry": "run-s clean build test refresh-npmrc-tokens",
    "publish:artifact-registry": "node ../../scripts/publish/artifact-registry.mjs --package-name cloud-scheduler-utils",
    "refresh-npmrc-tokens": "npx google-artifactregistry-auth --repo-config ../../.npmrc && cp ../../.npmrc .",
    "pretest": "run-s build",
    "size": "pkg-size ./lib --sort-by=brotli",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --selectProjects cloud-scheduler-utils --config ../../config/jest.cjs --rootDir ../../ --forceExit",
    "test:ci": "npm run test -- --ci --coverage",
    "test:watch": "npm run test -- --watch"
  },
  "dependencies": {},
  "peerDependencies": {
    "@google-cloud/scheduler": ">=2.x"
  },
  "devDependencies": {}
}
