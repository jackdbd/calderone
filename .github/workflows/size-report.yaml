name: 'size-report'

on:
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  package-size-report:
    name: 🔧 Build all packages [${{ matrix.os }}, Node.js ${{ matrix.node }}]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: ['current']
    steps:
      - name: 🛎️ Check out repo
        uses: actions/checkout@v3

      - name: ⚙️ Set up Node.js ${{ matrix.node }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}

      - name: ⚙️ Install dependencies
        run: npm ci

      - name: 🔧 Build libraries
        run: npm run build:libs

      - run: npm run size

      # The technique described here is no longer allowed.
      # https://www.edwardthomson.com/blog/github_actions_15_sharing_data_between_steps.html
      # TODO read this
      # https://github.blog/changelog/2020-10-01-github-actions-deprecating-set-env-and-add-path-commands/
      # - name: 📝 size report @jackdbd/checks
      #   run: |
      #     export SIZE_CHECKS=$(npm run size -w packages/checks)
      #     echo "::set-env name=SIZE_CHECKS::$SIZE_CHECKS"

      # - name: Add size report to PR
      #   https://github.com/marketplace/actions/add-pr-comment
      #   uses: thollander/actions-comment-pull-request@v1
      #   with:
      #     message: ${SIZE_CHECKS}
      #     reactions: eyes, rocket
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Why is this so slow? It went from a couple of minutes to 8 minutes, then
      # to more than 13 minutes!
      # - name: 📝 Package size report
      #   # https://github.com/marketplace/actions/pkg-size-action
      #   uses: pkg-size/action@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     build-command: 'npm run build'
      #     comment-report: true
      #     display-size: 'uncompressed'
      #     # ignore all docs generated by TypeDoc, but not the ones generated by
      #     # api-extractor + api-documenter (because they are useful when doing
      #     # API design review)
      #     hide-files: 'docs/**'
      #     # hide-files: '*.{css,js,ts}.map'
